{
  "private": true,
  "workspaces": [
    "core",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspace=@preptex/core && npm run build --workspace=@preptex/cli",
    "test": "npm run test --workspace=@preptex/core"
  },
  "overrides": {
    "rollup": "4.53.3"
  }
}
