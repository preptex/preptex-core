{
  "private": true,
  "workspaces": [
    "core",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspace=@preptex/core && npm run build --workspace=@preptex/cli",
    "typecheck": "npm run build --workspace=@preptex/core && npm run typecheck --workspace=@preptex/core && npm run typecheck --workspace=@preptex/cli",
    "test": "npm run test --workspace=@preptex/core && npm run test --workspace=@preptex/cli",
    "type-test": "npm run type-test --workspace=@preptex/core",
    "docs": "npm run docs --workspace=@preptex/core",
    "docs:check": "npm run docs:check --workspace=@preptex/core",
    "format": "prettier --ignore-path .prettierignore --write .",
    "format:check": "prettier --ignore-path .prettierignore --check .",
    "pack:check": "npm pack --workspace=@preptex/core --dry-run && npm pack --workspace=@preptex/cli --dry-run",
    "check": "npm run typecheck && npm test && npm run type-test && npm run docs:check && npm run format:check"
  },
  "overrides": {
    "rollup": "4.53.3"
  }
}
